"""Environment variables configuration file template.

Copy this file to .env and modify the values according to your environment.
This file should NOT be committed to version control.
"""

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=secretaria-el-cano
DB_USERNAME=root
DB_PASSWORD=your_password_here
DATABASE_URL=mysql+mysqlconnector://root:your_password_here@localhost:3306/secretaria-el-cano

# Initialize database on startup (set to true only for first run)
INIT_DB=false

# Authentication Configuration
AUTH_COOKIE_NAME=falla_cookie
AUTH_SECRET_KEY=your_secret_key_here
AUTH_COOKIE_EXPIRY_DAYS=1

# Application Configuration
APP_NAME=Secretaría El Cano
APP_ICON=🔥
APP_LAYOUT=wide
LOGO_PATH=assets/logo.png

# Development Configuration
DEBUG=false